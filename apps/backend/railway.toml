[build]
builder = "railpack"
buildCommand = "corepack enable && corepack prepare pnpm@9.15.4 --activate && pnpm install --frozen-lockfile && pnpm --filter @shaxmatchi/backend build"

[deploy]
preDeployCommand = ["pnpm --filter @shaxmatchi/backend db:migrate"]
startCommand = "pnpm --filter @shaxmatchi/backend start"
healthcheckPath = "/health"
healthcheckTimeout = 100
